// Copy this file to firebase-config.js and fill in your own project's values
// from the Firebase console (Project settings → General → Your apps → SDK
// setup and configuration). firebase-config.js is gitignored so it never
// gets committed.
//
// For local development, that's all you need to do.
//
// For the GitHub Pages deployment, this file is generated automatically by
// the .github/workflows/deploy.yml workflow from repository secrets — you
// do not need to commit a real firebase-config.js.

window.firebaseConfig = {
    apiKey: "YOUR_API_KEY",
    authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
    projectId: "YOUR_PROJECT_ID",
    storageBucket: "YOUR_PROJECT_ID.firebasestorage.app",
    messagingSenderId: "YOUR_SENDER_ID",
    appId: "YOUR_APP_ID"
};
